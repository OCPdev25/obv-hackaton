# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.turbo/
.expo/
*.tsbuildinfo

# Expo prebuild output (Continuous Native Generation — regenerate with
# `npx expo prebuild -p ios --no-install`; not committed per Expo convention)
apps/mobile/ios/
apps/mobile/android/

# Env files (real values only; .env.example files are committed)
.env
.env.local
.env.*.local
*.local

# Convex local state
.convex/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Convex codegen is committed intentionally for this scaffold (CI has no
# Convex deployment); if that policy changes, uncomment:
# backend/convex/convex/_generated/
