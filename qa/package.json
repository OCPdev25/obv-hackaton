{
  "name": "@journal/qa-recording",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reusable flow-recording harness + evidence publishing for UI-changing PRs (standalone, like evaluation/ and security/)",
  "scripts": {
    "record": "bun src/record.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "playwright": "^1.50.0",
    "typescript": "~6.0.3"
  }
}
