{
  "name": "obv-hackaton",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "deploy:thin-path": "pnpm --filter @journal/backend exec convex deploy"
  },
  "devDependencies": {
    "turbo": "^2.10.13",
    "typescript": "~6.0.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@swc/core",
      "core-js",
      "unrs-resolver"
    ]
  }
}
