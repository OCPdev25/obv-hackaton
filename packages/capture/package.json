{
  "name": "@journal/capture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Integrated voice-capture pipeline (Arena integration slice): pure capture state machine with raw-first durable persistence and retry, deterministic extraction double, append-only raw-before-events entry store, wire codecs derived from canonical Encoded types, corpus adapter, and a journey CLI contract probe.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "journeys": "bun src/journey/run.ts"
  },
  "dependencies": {
    "@journal/domain": "workspace:*",
    "effect": "4.0.0-rc.115"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "~6.0.3"
  }
}
