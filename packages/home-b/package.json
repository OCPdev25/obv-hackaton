{
  "name": "@journal/home-b",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Parent-home candidate B (Today feed-first): deterministic extraction double, in-memory capture store with raw-before-events + captureId idempotency, fail-closed authorization, and the synthetic Sep 2026 household dataset",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@journal/domain": "workspace:*",
    "@journal/extraction": "workspace:*",
    "effect": "4.0.0-rc.115"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "4.1.11"
  }
}
