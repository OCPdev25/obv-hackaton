{
  "name": "@journal/retrieval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only remember-and-retrieve query path over the deterministic synthetic history corpus (slots 15/17 groundwork; query contracts are rule-2 proposals, not self-landed)",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@journal/domain": "workspace:*",
    "effect": "4.0.0-rc.115"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "~6.0.3"
  }
}
